enum ConfectioneryType {
    Chocolate = "Chocolate",
    Cookies = "Cookies",
    Biscuits = "Biscuits",
    Candies = "Candies",
    Cakes = "Cakes"
}

enum DropDownType {
    normal,
    price
}

enum FilterType {
    Brand,
    Country,
    Quantity,
    Weigth,
    Availability
}

enum StockType {
    In,
    Out,
}

enum MathSign{
    Plus,
    Minus
}

export {ConfectioneryType, DropDownType, FilterType, StockType, MathSign}